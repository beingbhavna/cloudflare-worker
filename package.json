{
  "name": "vss-enterprise-cloudflare-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute vss-enterprise-db --file=schema.sql --remote",
    "db:local": "wrangler d1 execute vss-enterprise-db --file=schema.sql --local"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
